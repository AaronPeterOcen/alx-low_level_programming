#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int m = 10, a;

	for (int i = 0; i < m; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
