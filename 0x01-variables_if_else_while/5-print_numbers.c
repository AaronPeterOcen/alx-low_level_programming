#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int m = 9, a = 0;

	while (a <= m)
	{
		printf("%d", a);
		a++;
	}
	printf("\n");
	return (0);
}
