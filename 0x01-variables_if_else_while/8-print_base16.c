#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int m;

	while(m == 0 && m  < 10)
	{
		putchar(m);
		m++;
	}
	while(m = 'a' && m < 'f')
	{
		putchar(m);
		m++;
	}
	putchar('\n');

	return (0);
}
