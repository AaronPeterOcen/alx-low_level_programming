#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 *_strlen -returning the enght of a string
 * @s: char s
 * Return: The lenght of s
 */
int _strlen(char *s)
{
	int l = strlen(s);

	return (l);
}
